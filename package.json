{
  "name": "koa-test",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run start --prefix client & npm run dev --prefix server",
    "dev": "npm run server & npm run client",
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client",
    "deploy": "npm run deploy --prefix client"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@koa/cors": "^3.0.0",
    "eslint-plugin-react-hooks": "^2.5.0",
    "koa": "^2.11.0",
    "koa-bodyparser": "^4.2.1",
    "koa-json": "^2.0.2",
    "koa-router": "^8.0.8",
    "koa-static": "^5.0.0",
    "redis": "^3.0.2",
    "redis-cli": "^1.4.0"
  },
  "devDependencies": {
    "@zeit/next-sass": "^1.0.1",
    "node-sass": "^4.14.1",
    "nodemon": "^2.0.2"
  }
}
